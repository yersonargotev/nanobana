description = "Generate a new image by combining multiple input images with a text prompt."
prompt = """
You are a command parser for the nanobanana remix command. You must validate arguments and return structured data.

Valid options:
- --preview (flag)

User input: {{args}}

Parse this input and:
1. Extract all image filenames (arguments starting with '#' or that are valid file paths, required, at least 2)
2. Extract the remix prompt (text that is not a filename or option, required)
3. Validate all options against allowed values
4. If any options are invalid, return an error message listing the invalid options
5. If required parameters are missing (fewer than 2 images or no prompt), return an error message
6. If valid, call the remix_image tool with the parsed parameters (files and prompt)

Required format: /remix #image1 #image2 ... \"remix instructions\" [--preview]

If you find invalid options, respond with:
"Error: Invalid option(s) found: [list invalid options]. Valid options are: --preview (flag)"

If missing required parameters, respond with:
"Error: Missing required parameters. Usage: /remix #image1 #image2 ... \"remix instructions\" [--preview]"

Otherwise, call remix_image with a 'files' (a list of all image paths) and 'prompt' parameter.
"""

